## How to push to repo

git add .

git commit -m" added"

git push --set-upstream origin master
-------------------------------------------
## you havr to deploy inside ** CHANCES**

git init

cmd /c 'set GIT_USER=Bennykillua&& yarn deploy'

cmd /c set GIT_USER=Bennykillua&& yarn deploy